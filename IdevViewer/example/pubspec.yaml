name: idev_viewer_example
description: IDev Viewer 패키지 예제 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # IdevViewer 패키지 의존성 (로컬 패키지)
  idev_viewer:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/test-template.json
    
  # idev_viewer 패키지의 idev-app assets도 포함
  # (package dependency로 자동 포함됨)